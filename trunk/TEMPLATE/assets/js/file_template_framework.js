var Digu = window.Digu || {};
Digu.util = Digu.util || {};

/**
 * method description.
 *
 * @method  aMethod
 * @param   {String}    i   parameter description.
 * @return  {Boolean}       return description.
 */
Digu.aMethod = function(i) {
    return !!i;
};

/**
 * method description.
 *
 * @method  _init
 * @private
 */
Digu._init = function() {

};

/**
 * class description.
 *
 * @class   Digu.aClass
 * @static
 */
Digu.aClass = Digu.aClass || {
    /**
     * property description.
     *
     * @property    aProperty
     * @type        Object[]
     * @default     []
     */
    aProperty:[]
};

/**
 * constructor description.
 *
 * @namespace   Digu.util
 * @class       AConstructor
 * @param       {String}    name    parameter description.
 * @constructor
 */
Digu.util.AConstructor = function(name) {
    this.name = name;
};